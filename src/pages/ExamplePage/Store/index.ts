import { actions } from './actions';
import { getters } from './getters';
import { defaultState, mutations, IExampleState } from './mutations';
export {
  actions,
  getters,
  mutations,
  defaultState as state,
  IExampleState
};
